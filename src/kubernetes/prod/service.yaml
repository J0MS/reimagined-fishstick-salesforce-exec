apiVersion: v1
kind: Service
metadata:
  name: lead-score-platform-service
  namespace: salesforce-prod
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-type: nlb
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 8000
  selector:
    app: lead-score-platform
