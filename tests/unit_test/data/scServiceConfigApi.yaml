  expDetails:
    EXP_NAME: Kanishk-exp
    EXP_HYPOTHESIS: Test_API_Service
    EXP_STATUS: IN_PROGRESS
    EXP_TYPE: Portfolio
    EXP_MARKET: Mexico
    EXPERIMENT_START_DATE: 2022-09-01 #only YYYY-MM-DD Format allowed
    EXPERIMENT_END_DATE: 2022-09-30 #only YYYY-MM-DD Format allowed
    BASELINE_START_DATE: 2022-03-24 #only YYYY-MM-DD Format allowed
    BASELINE_END_DATE: 2022-08-31 #only YYYY-MM-DD Format allowed
    ANALYSIS_START_DATE: 2022-09-01 #only YYYY-MM-DD Format allowed
    ANALYSIS_END_DATE: 2022-09-30 #only YYYY-MM-DD Format allowed
    PRIMARY_OUTCOME_NAME: L_METRIC2 #Provide on of the values from L_METRIC 1 to 4 provided above
    SECONDARY_OUTCOME_NAME: 
    EXPERIMENTAL_FACTORS: 
    #provide all exp. factors here
    #for each group provide list of 3 elements:
    #       element 1: all control and treatment groups list, same groups should be provided in corresponding L_EXPFACT 1 to 5 columns in CSV
    #       element 2: At which number is the control group in above all groups list?
    #       element 3: Investment for each group
      factor-s: [[1, 0], 2, [0, 0]]
    #       Ex.: we only have 1st factor here "factor-r", it contains groups ['ctrl-a', 'test-a']
    #       ['ctrl-a', 'test-a'] should be mapped correctly in L_EXPFACT1
    #       Similarly if there is 2nd factor, provide them in correspoding L_EXPFACT2 column and so on
    CREATED_BY_NAME: Kanishk
    CREATED_BY_EMAIL: kanishk@ab-inbev.com
  
  executionDetails:
    MIN_DAYS_FOR_SC_EXEC: 7
    MIN_STRS_CNV_FOR_SC_EXEC: 250
    DYNA_CLUSTERING: True
    GENERALIZED_SC: False

  csvColumns:
    L_DATE: inv_date
    L_EXPUNIT: client
    L_ZIPCODE: zip
    L_COUNTRY: country
    L_BLCKFACT1: DRV
    L_BLCKFACT2: UEN
    L_BLCKFACT3: poc_segmentation
    L_BLCKFACT4: 
    L_BLCKFACT5:
    L_METRIC1: volume
    L_METRIC2: net_revenue
    L_METRIC3: maco
    L_EXPFACT1: test_group
    L_EXPFACT2: 
    L_EXPFACT3: 
    L_EXPFACT4: 
    L_EXPFACT5: 
    L_SKU: sku

  blockingFactors: [L_BLCKFACT1, L_BLCKFACT2, L_BLCKFACT3]